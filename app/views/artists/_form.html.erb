<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :artist_image %><br />
    <%= f.file_field :artist_image %>
  </div>
  <div class="field">
    <%= f.label :remote_artist_image_url %><br />
    <%= f.text_field :remote_artist_image_url %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
